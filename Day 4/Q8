#include <stdio.h>

int main() {
    int n;
    int sum;

    printf("Enter a number: ");
    scanf("%d", &n);

    // Using formula: sum = n * (n + 1) / 2
    sum = n * (n + 1) / 2;

    printf("Sum of first %d natural numbers is %d\n", n, sum);

    return 0;
}

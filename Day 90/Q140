#include <stdio.h>

enum Gender { MALE, FEMALE };

struct Person {
    char name[20];
    enum Gender g;
};

int main() {
    struct Person p = {"Rahul", MALE};
    printf("Name: %s, Gender: %s\n", p.name, p.g == MALE ? "Male" : "Female");
    return 0;
}
